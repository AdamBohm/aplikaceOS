# Copy this file to `.env` or set the variables in your environment.
# Do NOT store secrets in the repo. Fill these locally only.

# Flask
FLASK_SECRET=replace-this-with-a-random-secret

# SQL Server connection (example for pyodbc)
DB_DRIVER=ODBC Driver 18 for SQL Server
DB_HOST=your-sql-server-host
DB_PORT=1433
DB_NAME=your_database_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password
